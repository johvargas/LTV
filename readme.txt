## 02/11/2020

Running from console, you migh need to run install these packages previously.

py -m pip install requests
py -m pip install pandas
py -m pip install click

to Execute the program from cmd: 
- py main()

the previous, line will show the promp to enter the date.



## 01/11/2020
- currenly, the api_key and date is hardcoded.
- the database and table "user" needs to be previously created.

+ Runs from the main method.
	+ get the data form the API.
	+ insert the data into the sqlite db.
	
-- todo 
	- CLI projects (with date as argument)
	- validations on date
	- log messages
	- clean the code
	